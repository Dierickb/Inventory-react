const {BOOT_CENTER_CHANNELS} = require("./bootCenter")
const {BRANDS_CHANNELS, PRODUCTS_BRAND} = require("./brands")

module.exports = {
    BOOT_CENTER_CHANNELS,
    BRANDS_CHANNELS,
    PRODUCTS_BRAND,
}