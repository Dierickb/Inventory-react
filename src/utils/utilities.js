export const roles = {
    admin: "/admin",
    auth: "/dashboard",
    undefined: "/login",
    null: "/login",
}

export const eventButtonDeviceInfo = {
    event_update: true,
    event_send: true,
    null: false,
    undefined: false,
}
